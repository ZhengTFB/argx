/*
 * 文档页正文。
 *
 * 内容从 protocol/PROTOCOL.md、firmware/WIRING.md、sdk/AGENTS.md、sdk/README.md
 * **提取重写**而成，面向不懂硬件的人。
 *
 * 边界：firmware/WIRING.md 讲的是**装置侧接线**（怎么把 LED、蜂鸣器接到板子上），
 * 这里讲的是**电脑侧**（怎么让网页跟已经接好的装置说上话）——两边不重复。
 *
 * 放进数据文件而不是 JSX，理由和 works.ts 一样：加一页 = 加一条数据，不改组件。
 */

export type DocBlock =
  | { t: 'p'; text: string }
  | { t: 'h2'; text: string }
  | { t: 'h3'; text: string }
  | { t: 'ul'; items: string[] }
  | { t: 'code'; lang: string; code: string; head?: boolean }
  | { t: 'callout'; tone: 'info' | 'warning' | 'danger' | 'success'; text: string }
  | { t: 'table'; head: string[]; rows: string[][] };

export interface DocPage {
  slug: string;
  group: string;
  nav: string;
  title: string;
  lead: string;
  body: DocBlock[];
}

export const DOC_GROUPS = ['入门', '指南', '参考'] as const;

export const DOC_PAGES: DocPage[] = [
  /* ============================================================ */
  {
    slug: 'quickstart',
    group: '入门',
    nav: '快速开始',
    title: '三步让它动起来',
    lead: '不需要装驱动、不需要编译固件、不需要懂串口。装好之后，从零到灯亮大约两分钟。',
    body: [
      { t: 'h2', text: '1 · 插上装置' },
      {
        t: 'p',
        text: '用一根 USB **数据线**把开发板接到电脑。注意是数据线，不是纯充电线 —— 纯充电线里没有数据针脚，插上去板子会亮，但电脑看不到它。'
      },
      {
        t: 'callout', tone: 'warning',
        text: 'ESP32-S3-DevKitC-1 板子上有两个 USB-C 口。控制台认的是标着 USB 的那一个（原生 USB）。另一个（标着 UART 或 COM）只供电，插它设备页会一直连不上。两个口都试一下不算错，但记住是 USB 那个。'
      },
      { t: 'h2', text: '2 · 在控制台里连上它' },
      {
        t: 'p',
        text: '打开控制台的「设备」栏目，点「连真实装置（USB 串口）」。浏览器会弹出一个端口选择框 —— 那个框是**操作系统**画的，网页看不到里面有什么，所以控制台里没有"扫描到的设备列表"。选中带 USB 字样的那个端口即可。'
      },
      {
        t: 'p',
        text: '没有硬件也想先看看效果？点「连虚拟装置」。虚拟装置和真机跑的是**同一套会话层**，界面上看到的一切都一样，只是效果不会真的发生在你房间里。'
      },
      { t: 'h2', text: '3 · 验证它真的通了' },
      {
        t: 'p',
        text: '在设备页点「运行全测」。四路会依次各亮一下，每一路都要等装置把状态**回查回来**才算绿勾 —— 不是"我们发过了所以算过"。哪一路没接线，哪一路就会是黄叹号。'
      },
      {
        t: 'callout', tone: 'info',
        text: '绿勾的判断依据是 ARGX.state() 的应答。装置拔了线，本地就算记得发过指令也不会亮绿勾 —— 这正是自检有意义的原因。'
      },
      {
        t: 'code', lang: 'text', head: true,
        code: `设备页 → 连接 → 连真实装置 → 选端口
设备页 → 连接测试 → 运行全测
四路都绿 → 去 ARG 库挑一个作品`
      }
    ]
  },

  /* ============================================================ */
  {
    slug: 'connect',
    group: '入门',
    nav: '连接方法',
    title: '电脑侧怎么连上装置',
    lead: '这一页只讲电脑这一头：线、口、浏览器限制、连不上的时候先看哪里。装置那一头怎么接线，见仓库里的 firmware/WIRING.md。',
    body: [
      { t: 'h2', text: '浏览器要求' },
      {
        t: 'table',
        head: ['要求', '说明'],
        rows: [
          ['浏览器', '桌面版 Chrome 或 Edge（116 以上）。Firefox、Safari 都不支持 Web Serial'],
          ['地址', '必须是 localhost 或 HTTPS。用 file:// 直接打开页面时串口会被浏览器禁用'],
          ['设备', '桌面电脑。手机端浏览器不支持，这是浏览器的限制，不是这一版没做'],
          ['操作', '第一次连接必须由你**亲手点一次**按钮 —— 浏览器只允许在真实的点击里弹端口框']
        ]
      },
      {
        t: 'callout', tone: 'info',
        text: '最后一条常被误解成"这个按钮有时候不灵"。它其实是浏览器的安全设计：网页不能自己去翻你的串口设备。所以连接动作一定要从按钮点进去，中间不能插一段等待。'
      },
      { t: 'h2', text: '两个 USB 口的区别' },
      {
        t: 'table',
        head: ['口', '丝印', '能不能用', '说明'],
        rows: [
          ['原生 USB', 'USB', '✅ 用这个', '芯片自带的 USB 控制器，控制台靠它通信'],
          ['USB-UART 桥', 'UART / COM', '❌ 只供电', '板上那颗转换芯片，供电正常但控制台连不上']
        ]
      },
      {
        t: 'callout', tone: 'warning',
        text: '如果你是在 Arduino IDE 里刷固件，要用原生 USB 口时必须在 IDE 里把 USB CDC On Boot 设成 Enabled，否则串口是哑的：板子看着在跑，但一个字都不往外发。'
      },
      { t: 'h2', text: '为什么没有"扫描设备"这一步' },
      {
        t: 'p',
        text: '因为浏览器不给。Web Serial 只提供两个东西：requestPort()（弹一个由操作系统画的端口选择框，网页拿不到列表内容），和 getPorts()（只返回你以前授权过的端口）。所以"扫描到两台设备、每行一个连接按钮"这种界面做不出来 —— 那是能力边界，不是偷懒。'
      },
      { t: 'h2', text: '连上了会怎样' },
      {
        t: 'ul',
        items: [
          '顶部徽标从「未连接」变成「已连接」',
          '设备页出现设备 ID（形如 ARGX-0001）和它自报的能力清单',
          '右侧状态栏开始每 3 秒收到一次心跳，栏底显示往返毫秒数',
          '调试页的时间线开始出现收发的帧'
        ]
      },
      {
        t: 'callout', tone: 'danger',
        text: '如果 10 秒收不到装置的任何应答，会话层会判定掉线：状态栏四路回到待机、顶部徽标退化、右下角弹一条通知。这不是崩溃，是设计好的降级 —— 故事一行都不会因此中断。'
      }
    ]
  },

  /* ============================================================ */
  {
    slug: 'sdk',
    group: '入门',
    nav: '接入 SDK',
    title: '把 ARGX 接进你的作品',
    lead: '一份零依赖的单文件，整段复制进你已经做好的网页就行。不用重做游戏，不用构建工具。',
    body: [
      { t: 'h2', text: '三十秒接入' },
      {
        t: 'code', lang: 'html', head: true,
        code: `<!doctype html>
<meta charset="utf-8">
<script src="argx.js"></script>
<script>
  // 1. 初始化。没连装置、没有硬件、file:// 打开 —— 都静默降级，不抛错
  ARGX.init();

  // 2. 在剧情节点上发一个事件。作者只写事件名，不写能力 id 和参数
  document.querySelector('#reveal')?.addEventListener('click', () => {
    ARGX.fire('reveal');
  });
</script>`
      },
      {
        t: 'p',
        text: '就这两句。ARGX.init() 会自己找装置：控制台里嵌着跑的时候用宿主通道，独立打开时找串口，都没有就在 console 打一行日志。'
      },
      { t: 'h2', text: '作者只写事件名' },
      {
        t: 'p',
        text: '你不需要知道哪一盏灯配多亮、哪一个优先级会不会被抢。事件词表在 SDK 里配好了，你只写"这一刻发生的是什么"。内置六个：'
      },
      {
        t: 'table',
        head: ['事件', '用在什么时候', '它做了什么'],
        rows: [
          ['calm', '安定下来', '灯光缓慢降到 25%，氛围优先级'],
          ['tension', '不对劲了', '灯光 1.5 秒暗下去，氛围优先级'],
          ['reveal', '真相揭开', '灯光快速打到满 + 一声短响，高优先级'],
          ['danger', '危险逼近', '灯闪 + 长鸣 + 振动，高优先级'],
          ['relief', '松一口气', '灯光柔和回到 50%'],
          ['ending', '结局', '灯光常驻 90%，不再自动熄灭']
        ]
      },
      { t: 'h2', text: '自己定义事件' },
      {
        t: 'code', lang: 'js',
        code: `// 你的作品想要一个自己的组合
ARGX.defineEvent('door_lock', [
  { id: 'env.relay', p: { i: 1 } },
  { id: 'sound.beeper', p: { i: 0.8, dur: 120 } }
], { label: '门锁落下', desc: '继电器吸合 + 一声闷响' });

// 之后就像内置事件一样用
ARGX.fire('door_lock');`
      },
      {
        t: 'callout', tone: 'info',
        text: 'fire() 会按装置**实际声明**的能力过滤再发。所以"灯 + 蜂鸣 + 振动"这样的事件，在只有灯的装置上不会整条丢掉，而是只演灯那一部分。这是必须的：协议里 batch 是校验原子 —— 一条不合法整批都会被拒。'
      },
      { t: 'h2', text: '回查，而不是记账' },
      {
        t: 'code', lang: 'js',
        code: `// 想知道装置现在什么样，去问它
const st = await ARGX.state({ timeout: 900 });
if (!st) {
  // 没人应答：装置没连、或者这一帧丢了。
  // 这**不是错误**，你的剧情该照常往下走
  return;
}
console.log(st.out['light.main']); // { i: 0.8, pri: 2, ttl: 2400 }`
      },
      {
        t: 'callout', tone: 'danger',
        text: '剧情判定一行都不许依赖装置。装置只是"演"，玩家答对答错永远由你的网页决定。装置没插、线掉了、浏览器的端口框被点了取消 —— 故事都必须照常玩得下去。这是硬性要求，不是建议。'
      },
      { t: 'h2', text: '埋点要克制' },
      {
        t: 'ul',
        items: [
          '只在**情绪转折点**触发，一段文字里不要连发三次',
          '一个作品保留 3–8 个触发点就够，多了会变成迪厅',
          '把事件挂在**"玩家做成了什么"**的那一幕上，不要挂在标题页 —— 否则一进这一幕灯就闪，玩家还没做任何事',
          '不确定要不要加的时候，不加'
        ]
      }
    ]
  },

  /* ============================================================ */
  {
    slug: 'cues',
    group: '指南',
    nav: '指令与参数',
    title: '一条 cue 能带什么',
    lead: '网页发到装置的东西只有一种：cue —— 只描述"要什么感觉"，不含任何引脚电平。硬件怎么接是装置自己的事。',
    body: [
      { t: 'h2', text: '五个参数' },
      {
        t: 'table',
        head: ['参数', '类型', '缺省', '含义'],
        rows: [
          ['i', '0 ~ 1 小数', '1.0', '强度。超出范围会被钳住，不会报错'],
          ['dur', '毫秒整数', '30000', '持续多久。上限 30000'],
          ['ramp', '毫秒整数', '0', '渐入多久。超过 dur 会被钳到 dur'],
          ['pri', '0 ~ 3 整数', '2', '优先级，**数字越小越强**'],
          ['hold', 'true / false', 'false', '常驻：忽略 dur，直到被抢占或复位']
        ]
      },
      {
        t: 'code', lang: 'json', head: true,
        code: `{"v":1,"c":"cue","id":"light.main","seq":12,
 "p":{"i":0.8,"dur":3000,"ramp":800,"pri":2}}`
      },
      { t: 'h2', text: '连续量与开关量' },
      {
        t: 'p',
        text: '四路里有三路是**连续量**（灯 / 声音 / 振动），一路是**开关量**（继电器）。这个区别贯穿整个系统，不是界面上的装饰：'
      },
      {
        t: 'table',
        head: ['', '灯 / 声音 / 振动', '继电器'],
        rows: [
          ['强度', '有，0~1 连续可调', '只看 i ≥ 0.5 算吸合'],
          ['渐变 ramp', '有意义', '无意义 —— 物理上就是吸合与断开'],
          ['界面上', '滑杆 + 时长', '一个开关，没有滑杆'],
          ['切换动效', '有过渡（260ms）', '硬切（0ms）']
        ]
      },
      {
        t: 'callout', tone: 'warning',
        text: '声音这一路要特别说一句：**协议支持连续强度**（i 是 0~1 的小数，虚拟装置会按它做渐变），但**当前固件实现是二值的** —— i > 0.02 就输出 2.7kHz 方波，否则静音，音量不可调。所以同一段作品在真机上声音只有"响 / 不响"，在模拟器里能看到强度差异。这是固件实现到什么程度的问题，**不是协议设计的问题**；将来固件换成带 DAC 的驱动，这一路的强度就自然生效了，协议一行都不用改。'
      },
      { t: 'h2', text: '优先级：谁抢谁' },
      {
        t: 'table',
        head: ['值', '名字', '典型用途'],
        rows: [
          ['0', 'critical', '复位、看门狗。永远抢占，并且先清空全部输出'],
          ['1', 'high', '剧情关键节点：揭示、危险'],
          ['2', 'normal', '默认值'],
          ['3', 'ambient', '氛围底色，随时可以被抢']
        ]
      },
      {
        t: 'p',
        text: '规则只有一条：**新来的优先级不弱于当前的，就抢占；更弱，就丢弃**。被抢占的效果不会自己回来 —— 抢占不是暂停。'
      },
      { t: 'h2', text: '常驻（hold）' },
      {
        t: 'p',
        text: '默认的效果最长 30 秒就会自己熄灭，这是为了防止"网页崩了灯一直亮"。hold 放开的正是这个 30 秒限制：'
      },
      {
        t: 'code', lang: 'json',
        code: `{"c":"cue","id":"light.main","p":{"i":0.9,"ramp":800,"hold":true}}`
      },
      {
        t: 'ul',
        items: [
          '回查状态时 ttl 报 -1，表示"没有时限"',
          '被更高优先级抢占、被 reset、被看门狗清掉、断连 —— 四种情况都会释放它',
          'hold **不绕过看门狗**：15 秒没有有效帧，照样全部清空'
        ]
      },
      {
        t: 'callout', tone: 'info',
        text: '最后一条是刻意的。如果 hold 连看门狗都绕开，"玩家关掉浏览器，房间里的灯亮一整夜"这个事故就回来了。放开的是 TTL，不是安全兜底。'
      }
    ]
  },

  /* ============================================================ */
  {
    slug: 'ack',
    group: '指南',
    nav: '回执与去重',
    title: '装置回了什么',
    lead: '每一条被正常处理的 cue 都会回一个 ack。回执里带一个 r 字段，告诉你它到底执行了没有。',
    body: [
      { t: 'h2', text: '四种回执' },
      {
        t: 'table',
        head: ['r', '意思', '你该做什么'],
        rows: [
          ['applied', '执行了（含抢占）', '什么都不用做'],
          ['preempted', '执行了，并且抢掉了原来那个', '同上'],
          ['dup', '去重了，没重复执行', '同上 —— 这不是失败'],
          ['dropped', '按协议丢弃了（优先级不够）', '想让更强，把 pri 调小'],
          ['partial', 'batch 的回执，逐条结果在 res 里', '看 res 里谁被丢了']
        ]
      },
      {
        t: 'callout', tone: 'info',
        text: '为什么 dup 和 dropped 也要回 ack？因为不回的话，上层分不清"装置收到了但按规则没做"和"帧根本没到"，前者会一直被误判成掉线。'
      },
      { t: 'h2', text: '连点两下会怎样' },
      {
        t: 'p',
        text: '同一个能力、同样的优先级、**参数也一模一样**、而且上一条还在跑 —— 第二次会被判成重复点击，回 dup，不会重新执行。'
      },
      {
        t: 'p',
        text: '注意"参数也一模一样"这个条件。它是有意加的：如果只按 id + 优先级去重，氛围灯播放到一半你就调不了亮度了 —— 调亮度本来就是同 id 同优先级，会被当成重复点击吞掉。'
      },
      { t: 'h2', text: '一帧发多个' },
      {
        t: 'code', lang: 'json', head: true,
        code: `{"v":1,"c":"batch","seq":31,"p":{"cues":[
  {"id":"light.main","p":{"i":1,"ramp":120,"dur":1500}},
  {"id":"sound.beeper","p":{"i":1,"dur":250}}
]}}`
      },
      {
        t: 'ul',
        items: [
          '上限是 8 条、整帧不超过 512 字节',
          '**校验是原子的**：只要有一条 id 没注册，整批拒绝，一条都不执行',
          '执行时各条各自仲裁（优先级是当前状态的函数，校验阶段判定不了）',
          '回一个 ack，r 是共同结果或 partial，res 里逐条列出谁是什么结果',
          '**所有条目共用一个时间戳** —— 这是 batch 存在的全部理由'
        ]
      },
      {
        t: 'callout', tone: 'info',
        text: '为什么非要 batch：单独发两条 cue 之间隔着一次串口往返（十几个毫秒），"灯和声音同时起"做不到。要同时，就得在同一帧里。'
      }
    ]
  },

  /* ============================================================ */
  {
    slug: 'commands',
    group: '参考',
    nav: '指令表',
    title: '全部命令',
    lead: '一帧一行 JSON，换行定界。所有命令两端对等，谁都能发。',
    body: [
      { t: 'h2', text: '网页 → 装置' },
      {
        t: 'table',
        head: ['命令', '要带 id？', '做什么', '装置回什么'],
        rows: [
          ['hello', '否', '让装置报一下自己是谁、有什么能力', 'ready'],
          ['cue', '是', '触发一路效果', 'ack / err'],
          ['batch', '否', '一帧里同时触发多路（≤8 条）', '一个 ack（带 res） / err'],
          ['query', '否', '问现在什么状态', 'state'],
          ['cfg', '否', '配置（目前只有心跳间隔）', 'ack'],
          ['ping', '否', '心跳', 'pong'],
          ['reset', '否', '强制回空闲。critical，先清空全部输出', 'ack']
        ]
      },
      { t: 'h2', text: '装置 → 网页' },
      {
        t: 'table',
        head: ['帧', '什么时候发'],
        rows: [
          ['ready', '连上时、以及每次收到 hello'],
          ['ack', '每条被正常处理的命令'],
          ['state', '收到 query'],
          ['input', '装置上的物理输入被触发了（按键、开关）'],
          ['pong', '收到 ping'],
          ['err', '收到不能理解的命令或参数']
        ]
      },
      { t: 'h2', text: '公共字段' },
      {
        t: 'table',
        head: ['字段', '含义'],
        rows: [
          ['v', '协议版本，目前恒为 1'],
          ['c', '命令名'],
          ['id', '能力 id，形如 light.main（只有 cue 用）'],
          ['p', '参数对象 —— 所有行为都在这里面，顶层只做路由'],
          ['seq', '本条指令的序号，用于配对回执'],
          ['dev', '装置 id']
        ]
      },
      { t: 'h2', text: '什么样的行会被处理' },
      {
        t: 'ul',
        items: [
          '只处理以 { 开头的行。其余一律**静默丢弃**，不回 err —— 对垃圾回 err 会变成刷屏，把真正的错误淹掉',
          '以 # 开头的是调试日志，可以和帧混在同一条通道上，接收方直接忽略',
          '一帧最长 512 字节，超了丢弃并回 err:bad_frame',
          '换行用 \\n，\\r\\n 也容忍'
        ]
      },
      { t: 'h2', text: '能力声明长什么样' },
      {
        t: 'code', lang: 'json', head: true,
        code: `{"v":1,"c":"ready","dev":"ARGX-0001","proto":1,
 "caps":{"out":["light.main","sound.beeper","motion.vibrate","env.relay"],
         "in":[]}}`
      },
      {
        t: 'p',
        text: 'caps 里的 out / in 是**一串 id 字符串**，不带中文名、不带引脚、不带参数范围 —— 那些是协议的固定约定，不是装置报上来的。所以在控制台里看到的"灯光 / GPIO4 / 亮度·渐变·时长"是网页端查表补上的，不是装置说的。'
      },
      { t: 'h2', text: '状态帧长什么样' },
      {
        t: 'code', lang: 'json', head: true,
        code: `{"v":1,"c":"state","dev":"ARGX-0001","uptime":123456,
 "out":{"light.main":{"i":0.8,"pri":2,"ttl":2400},
        "sound.beeper":{"i":0,"pri":2,"ttl":0}},
 "in":{}}`
      },
      {
        t: 'ul',
        items: [
          'out 里**每一路都会列出**，没在跑的那路也在，i 为 0、ttl 为 0',
          'ttl 是剩余毫秒：0 表示没在跑，-1 表示常驻',
          'state 帧不带 seq，也不回 ack'
        ]
      }
    ]
  },

  /* ============================================================ */
  {
    slug: 'errors',
    group: '参考',
    nav: '错误码',
    title: 'err 帧的五个码',
    lead: '装置遇到不能理解的命令时回一个 err。它只是一个报告，会话不会因此断掉。',
    body: [
      {
        t: 'table',
        head: ['code', '什么时候出现', '怎么办'],
        rows: [
          ['bad_frame', '行以 { 开头但 JSON 坏了、缺必填字段、超过 512 字节、batch 为空或超过 8 条', '检查 JSON 是不是被截断了。串口上这种多半是发太快'],
          ['unknown_cmd', 'c 字段不是已知命令', '拼错了。注意大小写'],
          ['unknown_id', 'cue 里的 id 没注册过。整批 batch 会因此被拒', '向装置要一次 caps，或者用控制台设备页的能力列表核对'],
          ['unknown_input', '注入的 input id 不在 caps.in 里', '真机固件没有注册任何输入，所以这一条很常见 —— 见下面的说明'],
          ['bad_version', 'v 字段和装置的不兼容', '协议版本不一致，换一份匹配的实现']
        ]
      },
      {
        t: 'callout', tone: 'info',
        text: '收到 unknown_input 不代表你的链路坏了。恰恰相反：**帧到了装置、装置也回了话**，这才产生了这个错误。上行链路是通的。'
      },
      { t: 'h2', text: 'err 帧长什么样' },
      {
        t: 'code', lang: 'json',
        code: `{"v":1,"c":"err","code":"unknown_id","seq":12,
 "msg":"没有注册过这个能力"}`
      },
      {
        t: 'p',
        text: '注意 err 里**没有 id 字段**：出错的那个 id 不会回传，只能靠 msg 和 seq 去对。这是目前实现的一个已知缺口。'
      },
      { t: 'h2', text: '什么不会产生 err' },
      {
        t: 'ul',
        items: [
          '不以 { 开头的行 —— 静默丢弃，避免刷屏',
          '参数超出范围 —— 直接钳住（i 超了clamp 到 0~1，dur 超了钳到 30000）',
          '没连装置、file:// 打开、连接失败 —— 上层一律静默降级'
        ]
      }
    ]
  },

  /* ============================================================ */
  {
    slug: 'troubleshooting',
    group: '参考',
    nav: '故障排查',
    title: '不对的时候先看这里',
    lead: '按"从最可能到最不可能"排的。每条都先给判断方法，再给做法。',
    body: [
      { t: 'h2', text: '点连接，浏览器不弹端口选择框' },
      {
        t: 'ul',
        items: [
          '用的是 Chrome / Edge 桌面版吗？Firefox 和 Safari 不支持 Web Serial',
          '地址栏是 localhost 还是 https？file:// 下串口被禁用',
          '是不是重复点了？第一次弹框之后浏览器会记住授权，第二次可能直接连上不弹框',
          '按钮是不是在某个被 await 打断的调用栈里？中间插了等待，浏览器就认为这次点击已经过期了'
        ]
      },
      { t: 'h2', text: '连上了，但灯不亮' },
      {
        t: 'ul',
        items: [
          '先看右侧状态栏：触发时它有没有动？**动了**说明指令走通了，问题在接线那一头（去看 firmware/WIRING.md）',
          '**没动**说明帧根本没被处理：去调试页看时间线，`→` 那一行有没有发出去、`←` 那一行有没有 ack',
          '有 ack 但 r 是 dropped —— 优先级不够，被更强的一条压住了',
          '灯亮却看不出变化：检查限流电阻。LED 必须串 220~330Ω，不串会烧',
          '继电器不吸合：模块 VCC 必须接 5V / VIN，从 3V3 取电带不动'
        ]
      },
      { t: 'h2', text: '装了固件，装置没反应' },
      {
        t: 'ul',
        items: [
          '没用原生 USB 口。S3 板子上要插标着 USB 的那个口',
          'Arduino IDE 里 USB CDC On Boot 没开成 Enabled —— 板子在跑，但串口是哑的',
          '用的是老款 ESP32-WROOM-32E：它没有原生 USB，Web Serial 实际上连不上。这一版只保证它能编译通过',
          '板子一直在重启：多半是振动马达直连了 GPIO。马达必须经三极管或驱动模块，直连会烧引脚或触发欠压复位'
        ]
      },
      { t: 'h2', text: '界面显示"信号弱"或掉线' },
      {
        t: 'ul',
        items: [
          '确认数据线插紧了，换一根线试试（劣质线是常见原因）',
          '会话层每 3 秒发一次 ping；6 秒没应答标成 stale，10 秒判掉线。短暂 stale 是正常的，掉线才需要处理',
          '在调试页看心跳记录表：延迟那一列一直偏大，说明链路本身不稳'
        ]
      },
      { t: 'h2', text: '模拟器里明明好好的，换真机就不对' },
      {
        t: 'p',
        text: '两者走的是同一套会话层，所以差异一定出在**装置那一侧**：引脚接错、元件没接、供电不够。虚拟装置没有电气部分，它永远不会告诉你"电阻忘了串"。'
      },
      { t: 'h2', text: '调试页能做的几件事' },
      {
        t: 'ul',
        items: [
          '收发时间线：每一帧收发都在这里，可以只看错误',
          '手动发 cue：绕开你的作品，直接发一条看装置回什么',
          '故障注入：把装置弄坏（不发 ready / 丢包 / 垃圾串扰 / 半路断连），看界面撑不撑得住',
          'input 注入：验证反向通道（装置 → 网页）'
        ]
      }
    ]
  },

  /* ============================================================ */
  {
    slug: 'constants',
    group: '参考',
    nav: '常量总表',
    title: '一页记住所有数字',
    lead: '协议里所有定死的数字都在这。改任何一个都要改协议，不是改界面。',
    body: [
      {
        t: 'table',
        head: ['项', '值', '说明'],
        rows: [
          ['帧定界', '\\n', '\\r\\n 也容忍'],
          ['有效帧', '以 { 开头', '其余静默丢弃'],
          ['单帧上限', '512 字节', '超了丢弃并回 bad_frame'],
          ['心跳周期', '3000 ms', '网页发起'],
          ['判掉线', '10000 ms', '这么久没有 pong 就断'],
          ['看门狗', '15000 ms', '装置这么久没有有效帧就全部清空'],
          ['单条效果上限', '30000 ms', 'hold 例外'],
          ['batch 上限', '8 条', '整帧 512 字节'],
          ['协议版本', '1', ''],
          ['主输出引脚', 'GPIO4', '灯光'],
          ['调试日志前缀', '#', '与帧混在同一条通道上']
        ]
      },
      { t: 'h2', text: '四路与引脚' },
      {
        t: 'table',
        head: ['能力', '中文', '引脚', '驱动方式'],
        rows: [
          ['light.main', '灯光', 'GPIO4', 'PWM，5kHz，8 位'],
          ['sound.beeper', '声音', 'GPIO18', 'PWM，2.7kHz，实际只有开/关'],
          ['motion.vibrate', '振动', 'GPIO17', 'PWM，5kHz，必须经三极管'],
          ['env.relay', '继电器', 'GPIO16', '数字输出，高电平吸合']
        ]
      },
      {
        t: 'callout', tone: 'warning',
        text: '引脚不能随手改。GPIO5 是经典 ESP32 的 strapping 脚，GPIO6/7 在那块板上接内部 flash，接负载直接起不来；26–32 在 S3 上接 PSRAM。要改引脚，四处一起改：capabilities.h、WIRING.md、网页端的元件表，以及这里的表。'
      }
    ]
  }
];

export function docBySlug(slug: string): DocPage | undefined {
  return DOC_PAGES.find((p) => p.slug === slug);
}
