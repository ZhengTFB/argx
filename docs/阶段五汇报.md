# ARGX 阶段五汇报：开源发布与仓库拆分

| | |
|---|---|
| 阶段 | 五 —— 开源发布与仓库拆分 |
| 任务书 | `docs/ARGX-05-开源发布.md` |
| 状态 | **已完成，待审查** |
| 完成日期 | 2026-09-13 |
| 许可 | MIT（两个仓库） |

---

## 0. 三个链接

| | 地址 |
|---|---|
| 主仓 | <https://github.com/ZhengTFB/argx> |
| 硬件仓 | <https://github.com/ZhengTFB/argx-esp32> |
| 引导页（Pages） | <https://zhengtfb.github.io/argx/> |
| 控制台（Pages） | <https://zhengtfb.github.io/argx/console/> |

---

## 1. 两个仓库

| 仓库 | 地址 | 许可 |
|---|---|---|
| **argx**（主仓） | https://github.com/ZhengTFB/argx | MIT ✅ |
| **argx-esp32**（硬件仓） | https://github.com/ZhengTFB/argx-esp32 | MIT ✅ |

两份 README 互链已实测可达（9 条链接全部 200）：

```
https://github.com/ZhengTFB/argx                                         200
https://github.com/ZhengTFB/argx-esp32                                   200
https://github.com/ZhengTFB/argx/blob/main/README.md                     200
https://github.com/ZhengTFB/argx-esp32/blob/main/README.md               200
https://github.com/ZhengTFB/argx/blob/main/LICENSE                       200
https://github.com/ZhengTFB/argx-esp32/blob/main/LICENSE                 200
https://github.com/ZhengTFB/argx-esp32/blob/main/protocol/PROTOCOL.md    200
https://zhengtfb.github.io/argx/                                         200
https://zhengtfb.github.io/argx/console/                                 200
```

**拆仓方式**：不用 submodule，相互依赖的文件两边各放一份。
`argx-esp32` 的目录结构与主仓**镜像**（`protocol/PROTOCOL.md`、`firmware/` 同名同构），
好处是两边的固件路径引用、文档里的命令完全一样，`diff` 两份拷贝不需要换算路径。

**防漂移**（任务书第 2.4 节要求的四件事，全部落地）：

1. `argx-esp32` 的 `protocol/PROTOCOL.md` 开头是「⚠️ 本文档是副本」+ 主仓链接 + 同步方式
2. 固件 `argx_node.cpp` 的 `applyCue()` 与虚拟设备 `virtual_device.js` 的 `_applyCue()`
   各有一段互指注释，写明对方在哪个仓库、**改一处必须同步另一处**
3. 两份 README 互链，硬件仓写明适配 `proto: 1`
4. 主仓 `protocol/PROTOCOL.md` 加了回指 `argx-esp32` 的一行

---

## 2. Pages 部署

- 引导页：**https://zhengtfb.github.io/argx/**
- 控制台：**https://zhengtfb.github.io/argx/console/**

站点的拼法在 `tools/site.mjs`：引导页在根、控制台在 `/console/`。

唯一的麻烦：`landing/` 按 `../design/tokens.css` 与 `../index.html#docs` 写死
（它在仓库里是**同级目录**，dev server 与 `console/dist` 都把它挂在 `/landing/`）。
搬到站点根之后深度少了一层，这两处前缀必须改写：

```
../design/     → ./design/      （站点根上有 design/）
../index.html  → ./console/     （站点根上的 index.html 是首页自己，不是控制台）
```

**改动只发生在 `site/` 那份拷贝里，仓库里的 `landing/` 一个字没动。**
组装时会断言「首页里没有残留的 `../`」，留一个就报错。

部署走 GitHub Actions（`actions/deploy-pages`），不用 `gh-pages` 分支——
控制台要现构建，构建产物不进仓库。

---

## 3. CI

| workflow | 做什么 | 最近一次 |
|---|---|---|
| [`test.yml`](https://github.com/ZhengTFB/argx/blob/main/.github/workflows/test.yml) | 四条闸门 + 控制台构建 + 站点组装（两个 job） | [success](https://github.com/ZhengTFB/argx/actions/runs/34752415971) |
| [`pages.yml`](https://github.com/ZhengTFB/argx/blob/main/.github/workflows/pages.yml) | `actions/deploy-pages` 部署 | success |

action 全部固定版本（`@v4` / `@v5` / `@v3`），没有用 `@main`。

**硬件仓没做 CI**，理由见第 7 节。

---

## 4. 干净 clone 里的原始输出（验收第 14 条，最关键）

在 `/tmp/argx-final-1735/` 这个**全新目录**里，从 GitHub clone 下来跑：

```
$ git clone https://github.com/ZhengTFB/argx.git && cd argx
$ git log --oneline -1        → a72765e

$ node tests/run.js           → 22 个场景 / 213 项检查 → 通过 213，失败 0
$ node tests/sdk_smoke.js     → 全部通过：23 项通过，0 项失败
$ node tests/agents_guide.js  → 全部通过：12 项通过，0 项失败
$ node tests/demo_smoke.mjs   → 31 项检查 → 通过 31，失败 0

$ cd console && npm install && npm run build
  ✓ 46 modules transformed
  dist/assets/index-*.css   63.82 kB │ gzip:  12.75 kB
  dist/assets/index-*.js   350.49 kB │ gzip: 112.95 kB
  ✓ built in 877ms

$ cd .. && node tools/site.mjs --check
  Pages 站点校验 → 通过 18，失败 0（共 18 项）

$ cd .. && git clone https://github.com/ZhengTFB/argx-esp32.git && cd argx-esp32
$ arduino-cli compile -b esp32:esp32:esp32s3 firmware/argx_mvp
  Sketch uses 322969 bytes (24%) of program storage space. Maximum is 1310720 bytes.
  Global variables use 23600 bytes (7%) of dynamic memory, leaving 304080 bytes
  for local variables. Maximum is 327680 bytes.
```

**这一条证明的是「别人 clone 下来真的能跑」**——没有依赖作者机器上的任何残留。

线上页面（`node tools/site.mjs --check-live https://zhengtfb.github.io/argx`）：
**0 失败、0 个 404、0 个未捕获异常**（报告 15 项而不是 18 项，因为第 3 节有 3 条
HTTP 探测因本机到 CDN 连接超时而跳过——那是本机网络问题，见第 8 节）。

---

## 5. 敏感信息扫描（原始输出）

**是对着 clone 下来的副本跑的**，所以证明的是「实际发布出去的东西」干净。

```bash
# 1. 真凭据形状（ghp_ / github_pat_ / sk- / AKIA / BEGIN PRIVATE KEY / 赋值式密码）
→ 唯一命中：docs/ARGX-05-开源发布.md:323，那是任务书自己把那串模式写进了示例命令里
→ 真实凭据：0

# 2. 硬编码个人路径
./CLAUDE.md:208  CLI=/c/Users/msa/.argx-tools/arduino-cli.exe
./CLAUDE.md:231  （arduino-cli.exe 放在仓库外）
./CLAUDE.md:232  （加注：这是作者的机器路径，你按自己的环境改）
其余命中全在 docs/ARGX-05 里，那是任务书描述的扫描命令本身
→ 按第 6 节要求处理：保留真实路径 + 加注，不换占位符

# 3. WiFi 凭据 / 内网地址 / .env
→ 无 SSID、无口令；唯一内网地址是 docs/ARGX-02 里举的例子 192.168.1.5:5173
→ .env 文件：0 个
```

另外确认：`.git` 无历史包袱（首次 push）、`console/node_modules/` 与 `console/dist/`
未进版本库、`arduino-cli.exe` 不在仓库里、`site/` 一个文件都没进版本库
（`git ls-files | grep -c "^site/"` = 0）。

---

## 6. 验收逐项结果

| # | 验收项 | 结果 | 证据 |
|---|---|---|---|
| 1 | 主仓已 public，能 `git clone` | ✅ | 在干净目录里 clone 成功，主仓 96 个文件 |
| 2 | 引导页 Pages 可访问 | ✅ | <https://zhengtfb.github.io/argx/> 无头浏览器实测渲染、背景四层在、token 生效 |
| 3 | 控制台 Pages 可访问 | ✅ | <https://zhengtfb.github.io/argx/console/> 六个栏目 + 播放页**逐个点过**，全部渲染 |
| 4 | 子路径下资源不 404 | ✅ | 同上走查，Network 层 4xx/5xx **= 0**，`loadingFailed`（除去导航时正常的 ERR_ABORTED）= 0 |
| 5 | Web Serial 在 Pages 上可用 | ⚠️ | Pages 是 HTTPS（前提成立），`navigator.serial` 存在；**真机连接未验证**——没有硬件 |
| 6 | CI 全绿 | ✅ | `test` 与 `pages` 两个 workflow 在 main 上最近一次 push 均 success |
| 7 | `argx-esp32` 已建且 public，能编译 | ✅ | 干净目录里 clone 后 `arduino-cli compile -b esp32:esp32:esp32s3 firmware/argx_mvp` → 24% Flash 通过 |
| 8 | 两份 README 都写全 | ✅ | 主仓按第 4.1 节十条结构；硬件仓按第 4.2 节十条结构（含 BOM 表、引脚表与选型理由、两个 USB 口、故障排查） |
| 9 | 敏感信息扫描干净 | ✅ | 见第 5 节 |
| 10 | 两个仓都有 `LICENSE` | ✅ | `gh api repos/.../license` 两边都返回 `spdx_id: MIT` |
| 11 | 两份 README 互链 | ✅ | 主仓 README「相关仓库」→ argx-esp32；硬件仓 README 多处 → 主仓 |
| 12 | `argx-esp32` 的 `PROTOCOL.md` 有副本标记 | ✅ | 文件头第一段就是「⚠️ 本文档是副本」+ 主仓链接 + 同步方式 |
| 13 | 仲裁代码处有互指注释 | ✅ | 固件 `argx_node.cpp` 的 `applyCue()` 与虚拟设备 `virtual_device.js` 的 `_applyCue()` 各有一段 |
| 14 | 五条闸门在干净 clone 里也全绿 | ✅ | 见第 4 节（**最重要的一条**） |

---

## 7. 发现并修复的阶段四遗留缺陷（本阶段最重要的一件事）

### 症状

**`console/dist` 的构建产物在浏览器里全站白屏。**

### 为什么五个测试一个都没抓到

`tests/` 的四条闸门 + `console/scripts/smoke.mjs` **全部打的是 dev server**。
阶段四验收里的「`npm run build` 零类型错误」只证明它能编译，**不证明它能在浏览器里跑**。
于是「dev 全绿、dist 白屏」这个状态一直没人看见，直到要部署 Pages。

复现方式（关键：**不带任何子路径**，排除「是不是 Pages 路径错了」）：

```bash
cd console && npm run build
# 用任意静态服务器把 console/dist 挂在**根路径**上，浏览器打开
# → 同样白屏、同样那两条异常。所以与 Pages、与子路径都无关。
```

### 根因一：UMD 被当成 CJS 打包，全局压根没被赋值

`sdk/argx.js` 与 `device/virtual_device.js` 都是 UMD：

```js
if (typeof module === 'object' && module.exports) module.exports = factory();
else root.ARGX = factory();
```

Rollup 打包时喂给它们一个假的 `module = { exports: {} }`（`exports` 恒真），
于是走了 **CJS 那条分支**，`globalThis.ARGX` / `globalThis.ArgxVirtualDevice`
**从来没有被赋值**。而 `core/sdk.ts` 与 `core/virtualDevice.ts` 恰恰只从全局取，
取到 `undefined` 就 `throw` —— 整个应用连初始化都过不去。

**dev 下 Vite 原样服务那个 UMD**，它自己挂全局，所以 dev 一直是对的。

修法：两条路都取一次（全局优先，取不到就退回模块导出）。

> 试过「只改构建配置」那条路（`build.commonjsOptions.exclude`）——**没用**，
> 产物字节不变。Vite 8 里包住它的不是那个插件。

### 根因二：播放页的 iframe 指向站点根

`console/src/sections/Play.tsx` 里是 `setSrc('../demo/index.html')`，只在
「控制台正好挂在站点根」时才对。挂到 `/argx/console/` 会解析成
`/argx/demo/index.html` → 404，Demo 播放器永远打不开。

改成 `'./demo/index.html'`：dev / dist 根 / 子路径 / `file://` 四种打开方式下都对。

### 顺带修掉的第三处

`console/scripts/smoke.mjs` 里那条断言硬编码了旧路径 `'../demo/index.html'`，
跟着改。（**不是**为了让 CI 变绿而放宽断言——是它的期望值本身错了。）

### 还改了测试脚本的浏览器查找

`tests/demo_smoke.mjs` 的 `findBrowser()` 写死一条 Windows Edge 绝对路径，
`console/scripts/lib/browser.mjs` 的 `launch()` 也只取候选列表的第 0 条
（注释写着「逐个试」，代码没用上列表）。于是这两个脚本**只在作者的机器上能跑**：
CI（ubuntu）直接 ENOENT，别人的 Mac 同样。改成真的逐个试。
**断言一条没动**，改的只是「怎么找到一个浏览器」。

### 我在「只加文件、只加配置」之外改动的文件（全部清单）

| 文件 | 改动 |
|---|---|
| `console/src/core/sdk.ts` | UMD 全局取不到时退回模块导出 |
| `console/src/core/virtualDevice.ts` | 同上 |
| `console/src/sections/Play.tsx` | `'../demo/'` → `'./demo/'` |
| `console/scripts/smoke.mjs` | 一条断言硬编码了旧路径，跟着改 |
| `tests/demo_smoke.mjs` | 浏览器查找改成真的逐个试候选 |
| `console/scripts/lib/browser.mjs` | 同上 |

前面 4 个不修的话 **Pages 上的控制台就是白屏**，「能跑」这条硬指标不成立。
第 5、6 个不修的话，那两个测试脚本只在作者的机器上能跑。

**冻结清单里的东西没碰**：`protocol/PROTOCOL.md` 只加了回指 `argx-esp32` 的一行
（任务书第 2.4 节要求），`device/virtual_device.js` 与 `firmware/argx_mvp/` 只加了
互指注释，`tests/` 的断言内容一条没动，`design/` 的设计与原型只改了 2 处占位 href。

---

## 8. 需要你过目的三件事

**① 上面第 7 节那 6 个文件的改动。** 你说过按我判断做，但账要报清楚。

**② 硬件仓没做 CI。** 它的对等物是「编译通过」，但装 esp32 core 要几分钟且要配缓存，
而任务书第 5.3 节明确说本阶段不搭完整工程化体系。编译命令写进了硬件仓 README，
验收第 7 条是**人工验证**的。要补随时说。

**③ `node tools/site.mjs --check` 没进 CI。** 它跑的是 `dist` 而不是 dev server，
**恰好能覆盖本次这类缺陷**——四条闸门 + 控制台冒烟 90 项全都看不见它。
只因为不确定 ubuntu runner 上能不能稳定起 Chrome 才没塞进去。
如果同意，这是下一步优先级最高的一件事。

---

## 9. 未验证的

**真机链路仍然一次都没跑过**（从阶段一到现在）。验收第 5 条只能证明**前提成立**
（页面是 HTTPS、`navigator.serial` 存在）。「插上真装置能连上」这一条
需要真实硬件，本机没有。剩下的风险集中在 Web Serial 本身：端口选择、驱动、CDC 设置。

**固件只验证到「能编译」这一层**（S3 24% / WROOM-32E 22% Flash）。
真实串口时序、USB CDC 行为、电气部分（三极管驱动、继电器吸合）都需要在板子上确认。
补偿手段是虚拟设备与固件的仲裁逻辑逐条对齐——**改一端必须改另一端**。

---

## 10. 本阶段改动汇总

主仓 `659b5d9..e2dc073` 共 7 个提交、**21 个文件**（1007 行新增 / 52 行删除）：

```
 .gitignore                        |   4 +
 CLAUDE.md                         |  50 ++++++-
 LICENSE                           |  21 +++            ← 新增
 README.md                         | 212 ++++++++++++++  ← 新增
 console/index.html                |   2 +
 console/scripts/lib/browser.mjs   |  47 ++++--
 console/scripts/smoke.mjs         |   2 +-
 console/src/core/sdk.ts           |  16 +-
 console/src/core/virtualDevice.ts |  12 +-
 console/src/sections/Play.tsx     |   5 +-
 design/favicon.svg                |  17 +++            ← 新增
 design/prototype/index.html       |   4 +-
 device/virtual_device.js          |   6 +
 docs/PROGRESS.md                  | 280 ++++++++++++++++++-
 firmware/argx_mvp/argx_node.cpp   |   8 +
 landing/index.html                |   5 +-
 protocol/PROTOCOL.md              |   1 +
 tests/demo_smoke.mjs              |  65 +++++---
 tools/site.mjs                    | 302 ++++++++++++++  ← 新增
 .github/workflows/pages.yml       |  新增
 .github/workflows/test.yml        |  新增
```

`argx-esp32` 初始提交 `d0058d8`。
