# 依赖与构建产物：都不进仓库
node_modules/
console/dist/
.vite/
# tools/site.mjs 拼出来的 Pages 站点（每次重新生成）
# 注意：.gitignore 的注释必须独占一行 —— 写在规则后面会被当成规则的一部分，
# 结果就是这条规则失效（这个坑踩过一次）。
site/

# 编辑器与系统
.DS_Store
Thumbs.db
*.log
